# mongodb.yaml

net:
  bindIp: 127.0.0.1
  port: 27017

storage:
  dbPath: /var/lib/mongodb
  journal:
    enabled: true

systemLog:
  destination: file
  path: /var/log/mongodb/mongod.log
  logAppend: true
